#!/bin/zsh

clear
touch tempfile
ls src > tempfile

for {pegar todas as linhas uma por uma do arquivo tempfile} in tempfile
do
    if [ {nome do arquivo encontrado na linha} -d {for um diretório}
    then
    grep {nome do arquivo} public static void main(String]
    then
        DIR_MAIN={nome do diretório que possui arquivo com método main}
        CLASS_MAIN={nome do arquivo encontrado dentro do pacote que contém o método main
      fi
$DIR="src/" + {nome do diretório} + "/*.java "
$(STRING)+= $DIR

E eu ainda não pensei para quando não houverem pacotes (quando arquivo não for diretório)
