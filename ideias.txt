1 - verificar se há uma pasta com src no diretório atual.
1.1 - caso não haja, efetuar algoritmo que volta eliminando o último diretório até encontrar a pasta src.
1.2 - caso haja, verificar procurar por arquivos com caracteristicas de projetos Maven ou Gradle.
1.2.1 - caso ache, executar conforme o tipo de projeto.
1.3 - caso ache apenas a pasta src,  
2.a.1 - se não houver diretório (logicamente serão arquivos com extensão .java).
2.a.2 - verificar se há mais de um arquivo no diretório.

3.a.1 - se houver somente um arquivo, executar o mesmo.
3.a.2 - se houver mais de um arquivo, iterar sobre os arquivos pesquisando pelo arquivo que contém o método main.
3.a.3.1 - se apenas um arquivo contiver o método main, então o deverá ser executada função para voltar para o diretório raiz
e então compilar e executar o programa.
3.a.3.2 - se houver mais de um arquivo com o método main, então deverá ser exibido uma lista com os arquivos contendo o método main,
exibindo um prompt perguntando ao usuário qual arquivo deverá ser executado.  

3.b.1 - se houver diretório, então deverá ser executada função para entrar dentro dos diretórios (talvez usando um algoritmo de
recursividade), procurando pelo diretório e pelo arquivo que contém a função main.
3.b.2 - compilar e executar o programa usando as informações coletadas (pacote e arquivo com método main).

1 - listar todos os arquivos do diretório atual. 
2 - verificar se são pastas ou arqiuvos com extensão .java.
2.2 - verificar se há apenas um arquivo com extensão .java.
2.2.1 - se houver apenas um arquivo, verificar se há o método main.
2.2.2 - se houver o método main, verificar se está importando algum pacote.
2.2.3 - se não estiver imporado nenhum pacote, executar o arquivo. 
2.3 - se houver mais de um arquivo, verificar se mais de um arqiuvo contém o método main.
2.3.1 - se mais de um arqiuvo contiver o método main, listar os arquivos com o método main e perguntar ao 
usuário qual arquivo deverá ser executado. 
2.3.2 - se apenas um arquivo contiver o método main, guardar o nome do arqiuvo e seu caminho relativo em variáveis, e 
executar função que volta diretório para verificar se o arquivo está dentro de algum pacote. (se chegar até a raiz do projeto,
ou seja, diretório que contém a pasta src, sem encontrar nenhum pacote, então usar o caminho para listar todos arquivos do
diretório que contém o método main, compilar e executar o código.
2.3.3 - se na volta encontrar algum pacote, então efetuar função para compilar o código usando todos os pacotes encontrados.
(irá executar ainda da pasta src).
2.3.4 - se não houver nenhum arquivo com o método main, executar duas funções, uma para entrar mais a fundo nos diretórios,
e outra para voltar para o diretório procurando por outros diretórios, a fim de encontrar o método main.
2.3.5 - ao encontrar o método main, quardar o pacote e o arquivo com o método main, e executar função para voltar para o 
diretório src.

OBS: ao efetuar a função para voltar para o diretório src pela primeira vez, verificar se não há nenhum arquivo que remeta
há um projeto pré-instalado, como Maven ou Gradle (procurando por arquivos como pom.xml ou build.gradle na raiz do projeto)
se houver algum desses arquivos, parar a execução de procura do programa e executar o programa conforme o tipo de projeto.
